# 案例复盘：脚本“副作用”引发的 GMSL 视频流丢失故障

**背景：** 这是一个典型的“看似软件逻辑 Bug，实则底层状态污染”的硬件相关排错案例。

## 1. 现象：消失的左路视频
在系统加入“启动自动导出内参”的功能后，出现了一个诡异现象：
- **偶发变确定：** 只要跑过内参脚本，再开采集，左侧 `/dev/video2` 极大概率不出流，而右侧 `/dev/video3` 却总是好的。
- **版本分水岭：** 只有合入了内参脚本的版本才有问题，回退即消失。

## 2. 排查：从“应用层”到“寄存器”
最初我怀疑是 GStreamer 管道丢帧或者编码器卡死，但通过 `v4l2-ctl` 直接抓原始流发现：**根本没有物理信号进来。**

这说明问题不在软件处理逻辑，而是链路断了。我顺着脚本逻辑往下挖：
- **脚本干了什么？** 它在读传感器内参时，为了防止 I2C 冲突，会用 `i2ctransfer` 去改解串器（Deser）的 `0x0006` 寄存器，以此来切不同的接收通道。
- **发现猫腻：** 读一下寄存器现场，发现脚本跑完后，`0x0006` 的值停在了 `0xF8`（只开最后一路），而不是初始的 `0xFF`（全通）。

## 3. 定位：为什么受灾的总是“左路”？
分析脚本逻辑后发现，这完全是一个**“时序留下的坑”**：
- 脚本是按顺序扫相机的：左路 -> 右路。
- 扫左路时，寄存器设为左路选通；扫右路时，设为右路选通。
- **重点：** 脚本运行完就直接退出了，没有把寄存器还原。

**结果就是：** 硬件状态被锁死在了最后一次操作（右路）上。采集程序启动时，左路通道在物理层其实是闭塞的。

## 4. 解决：做个“有素质”的工具
- **改脚本：** 引入 `try...finally` 机制。不管脚本读取成功还是失败，退出前必须把 `0x0006` 还原回初始状态，实现“无痕运行”。
- **补防御：** 在采集主程序的初始化逻辑里，增加了一步对关键路由寄存器的强制复位，不再盲目相信硬件的当前状态。

## 5. 经验提炼（面试的核心干货）
- **工具脚本必须“事务化”：** 在嵌入式开发中，任何改写硬件状态的工具（Python/Shell）都必须遵循“读取备份 -> 修改 -> 还原”的闭环。**不留 Side Effect（副作用）是底层开发的基本素质。**
- **建立“状态快照”思维：** 面对这种跨进程、跨阶段的 Bug，单看代码逻辑是没用的。我当时的突破点就是直接比对故障前后的寄存器 Dump 图谱，硬件不会撒谎。
- **区分“路径”与“源头”：** 视频不出流（现象）-> 节点无数据（路径）-> 选通掩码错误（源头）。排错时要快速在协议层（I2C/GMSL）定性，避免在应用层（编解码/业务逻辑）浪费时间。
